import React, { FC, useState } from 'react';
import { ProductForm } from '../../features/forms/ProductForm';

export const ProductAddScreen: FC = () => {
  return (
    <div>
      <ProductForm />
    </div>
  );
};
