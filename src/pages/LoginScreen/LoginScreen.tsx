import React, { FC } from 'react';
import { LoginForm } from '../../features/forms/LoginForm/index';

export const LoginScreen: FC = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};
